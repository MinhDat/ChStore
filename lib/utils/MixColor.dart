import 'package:flutter/material.dart';

class MixColor {
  final Color main = Color(0xFFFF2D55);
  final Color unfocused = Colors.black54;
  final Color counterButton = Colors.redAccent[100];
  final Color lineColor = Colors.grey;
  final Color completeColor = Colors.green[400];
}

var mixColor = MixColor();
